# Changelog

## 1.0.0
- Initial release.
- The vanilla POCKET C.A.R.T. matches the normal C.A.R.T.'s width (measured at runtime) and is twice as deep. Colliders, in-cart detection, handle and push distance are adjusted to match.
- Host-controlled: guests use the host's published size and stay vanilla if the host doesn't run the mod.
